# =============================================================================
# Auto Pilot — Environment Variables Template
# =============================================================================
# INSTRUCTIONS (Local Development):
#   1. Copy this file to .env:
#        cp .env.example .env
#   2. Fill in TOKEN_ENC_KEY with a real Fernet key (see below).
#   3. NEVER commit .env — it is blocked by .gitignore.
#
# ZEABUR DEPLOYMENT:
#   Do NOT use a .env file on Zeabur.
#   Paste each variable directly into the Zeabur project dashboard:
#       Project → Variables → Add Variable
#   This keeps secrets out of the Docker image and out of the repository.
# =============================================================================

# --- Fernet Encryption Key (REQUIRED) ---
# Encrypts/decrypts API keys stored in the local SQLite vault.
# Generate once with:
#   python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
# Then paste the output here (or into Zeabur Variables).
TOKEN_ENC_KEY=your_fernet_key_here

# --- OAuth Redirect URI (optional — Webmaster / GSC / Bing only) ---
# Set to your public domain when deployed (e.g. https://autopilot.zeabur.app)
# OAUTH_REDIRECT_BASE=http://localhost:8501
