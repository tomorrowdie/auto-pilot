# =============================================================================
# Omni Traffic System — Python Dependencies (V5 Stack)
# =============================================================================

# --- Core Framework ---
streamlit>=1.28.0

# --- Data Processing ---
pandas>=2.0.0
numpy>=1.21.0
pyarrow>=14.0.0
openpyxl>=3.1.0

# --- UI Components ---
streamlit-aggrid==1.0.5

# --- HTTP / Networking ---
requests>=2.28.0

# --- Google OAuth & API (Webmaster / GSC) ---
google-auth>=2.23.0
google-auth-oauthlib>=1.1.0
google-auth-httplib2>=0.1.1
google-api-python-client>=2.100.0

# --- Encryption (Fernet vault for API key storage) ---
cryptography>=41.0.0

# --- Environment ---
python-dotenv>=1.0.0

# --- LangChain V5 Stack ---
# Core abstractions (Messages, Runnables) — required by all providers
langchain-core>=0.3.0
# Provider integrations (lazy-imported in registry.py)
langchain-openai>=0.3.0          # OpenAI, DeepSeek, Alibaba (OpenAI-compatible)
langchain-anthropic>=0.3.0       # Anthropic (Claude)
langchain-google-genai>=2.0.0    # Google Gemini
langchain-ollama>=0.3.0          # Local Ollama
